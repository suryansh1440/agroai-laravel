services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: agro-ai-app
    container_name: agro-ai-app
    restart: unless-stopped
    ports:
      - "8004:80"
    env_file:
      - .env
